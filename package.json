{
  "name": "quanlypj",
  "version": "1.0.0",
  "private": true,
  "description": "Student Project Management System",
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "start": "npm start --prefix backend",
    "build": "npm run build --prefix frontend",
    "prisma:generate": "npm run prisma:generate --prefix backend",
    "prisma:migrate": "npm run prisma:migrate --prefix backend",
    "seed": "npm run seed --prefix backend",
    "smoke:test": "npm run smoke:test --prefix backend",
    "db:clear": "npm run db:clear --prefix backend",
    "db:fix-sequences": "npm run db:fix-sequences --prefix backend"
  }
}
